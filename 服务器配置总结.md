# 🖥️ 服务器配置总结

## 📊 服务器架构

### 服务器1：前端管理页面（admin-web）
- **IP地址**：`118.25.70.79`
- **服务**：Web管理后台（React + TypeScript）
- **端口**：`80` (HTTP)
- **访问地址**：http://118.25.70.79
- **用途**：
  - 饿了么数据分析系统
  - 店务管理后台
  - 数据上传和编辑

### 服务器2：后端API服务器（backend）
- **IP地址**：`118.89.73.199`
- **服务**：后端API服务（Flask + Python）
- **端口**：`5000`
- **API地址**：http://118.89.73.199:5000
- **用途**：
  - 用户认证和授权
  - 工单管理API
  - 培训系统API
  - 店务管理API
  - **隐私政策页面托管**

---

## 🔗 各端配置情况

### 1. 前端移动应用（Flutter App）

**配置文件**：`frontend/lib/core/config/environment.dart`

```dart
/// 生产环境配置
static const String productionApiUrl = 'http://118.89.73.199:5000';
```

**iOS 网络权限**：`frontend/ios/Runner/Info.plist`

```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSExceptionDomains</key>
    <dict>
        <key>118.89.73.199</key>  <!-- 后端API服务器 -->
        <dict>
            <key>NSExceptionAllowsInsecureHTTPLoads</key>
            <true/>
        </dict>
    </dict>
</dict>
```

**连接关系**：
- APP → 后端API服务器 (`118.89.73.199:5000`)

---

### 2. Web管理后台（admin-web）

**配置文件**：`admin-web/src/api/config.ts` （需确认）

应该配置为：
```typescript
const API_BASE_URL = 'http://118.89.73.199:5000';
```

**部署位置**：`118.25.70.79`

**连接关系**：
- 用户访问：`http://118.25.70.79`
- admin-web → 后端API服务器 (`118.89.73.199:5000`)

---

### 3. 隐私政策页面

**部署位置**：后端服务器 (`118.89.73.199`)

**访问地址**：
```
http://118.89.73.199:5000/privacy
```

**文件位置**：
- HTML文件：`~/SunpizzaApp/backend/static/privacy.html`
- 路由配置：`~/SunpizzaApp/backend/wsgi.py`

---

## 📱 App Store Connect 配置

在提交iOS应用到App Store时，需要填写：

| 字段 | 值 |
|------|-----|
| **隐私政策URL** | `http://118.89.73.199:5000/privacy` |
| **支持URL** | （可选）`http://118.25.70.79` |
| **营销URL** | （可选）公司官网 |

---

## 🔧 部署命令快速参考

### 部署前端管理页面 (admin-web)

```powershell
cd C:\Users\YQH20\Desktop\SunpizzaApp\admin-web

# 构建
npm run build

# 上传到前端服务器
scp -r dist/* ubuntu@118.25.70.79:~/SunpizzaApp/admin-web/dist/
```

### 部署隐私政策页面

```powershell
cd C:\Users\YQH20\Desktop\SunpizzaApp\backend

# 使用自动化脚本
.\deploy_privacy.ps1

# 或手动部署
scp static/privacy.html ubuntu@118.89.73.199:~/SunpizzaApp/backend/static/
scp wsgi.py ubuntu@118.89.73.199:~/SunpizzaApp/backend/
ssh ubuntu@118.89.73.199 "sudo systemctl restart sunpizza-backend"
```

### 部署后端代码更新

```powershell
# 上传代码到后端服务器
scp -r app/* ubuntu@118.89.73.199:~/SunpizzaApp/backend/app/

# 重启服务
ssh ubuntu@118.89.73.199 "sudo systemctl restart sunpizza-backend"

# 查看日志
ssh ubuntu@118.89.73.199 "sudo journalctl -u sunpizza-backend -f"
```

---

## ✅ 配置验证清单

### 前端移动应用
- [ ] `environment.dart` 中配置了正确的后端地址
- [ ] `Info.plist` 中配置了 HTTP 例外域名
- [ ] 能成功登录和获取数据

### Web管理后台
- [ ] 能访问 `http://118.25.70.79`
- [ ] 能成功调用后端API
- [ ] 数据上传和查询功能正常

### 隐私政策
- [ ] 能访问 `http://118.89.73.199:5000/privacy`
- [ ] 页面样式正常显示
- [ ] 内容完整可读

### 后端服务
- [ ] 服务运行正常：`sudo systemctl status sunpizza-backend`
- [ ] 端口5000可访问
- [ ] API响应正常

---

## 🔍 常用检查命令

### 检查前端服务器

```powershell
# 查看文件
ssh ubuntu@118.25.70.79 "ls -lh ~/SunpizzaApp/admin-web/dist/"

# 检查nginx配置
ssh ubuntu@118.25.70.79 "sudo nginx -t"
ssh ubuntu@118.25.70.79 "sudo systemctl status nginx"
```

### 检查后端服务器

```powershell
# 查看服务状态
ssh ubuntu@118.89.73.199 "sudo systemctl status sunpizza-backend"

# 查看日志
ssh ubuntu@118.89.73.199 "sudo journalctl -u sunpizza-backend -n 50"

# 测试API
curl http://118.89.73.199:5000/health

# 检查隐私政策
curl http://118.89.73.199:5000/privacy
```

---

## ⚠️ 注意事项

### 1. 关于HTTP
- 当前两台服务器都使用 HTTP 协议
- **建议**：生产环境配置 HTTPS 证书（Let's Encrypt）

### 2. 防火墙
确保防火墙开放以下端口：
- 前端服务器：`80` (HTTP)
- 后端服务器：`5000` (API)

### 3. 跨域配置
后端需要配置 CORS，允许来自以下来源的请求：
- `http://118.25.70.79` (admin-web)
- iOS App 请求

### 4. 安全建议
- [ ] 配置HTTPS
- [ ] 配置域名（避免直接使用IP）
- [ ] 设置防火墙规则
- [ ] 定期更新SSL证书
- [ ] 定期备份数据库

---

## 📞 故障处理

### admin-web 无法访问
1. 检查nginx服务：`sudo systemctl status nginx`
2. 检查文件是否存在：`ls ~/SunpizzaApp/admin-web/dist/`
3. 查看nginx错误日志：`sudo tail -f /var/log/nginx/error.log`

### 后端API无响应
1. 检查服务状态：`sudo systemctl status sunpizza-backend`
2. 查看错误日志：`sudo journalctl -u sunpizza-backend -n 100`
3. 检查端口占用：`sudo netstat -tlnp | grep 5000`
4. 重启服务：`sudo systemctl restart sunpizza-backend`

### APP无法连接后端
1. 确认后端服务运行正常
2. 检查 `environment.dart` 配置
3. 确认 `Info.plist` 配置了 HTTP 例外
4. 检查网络连接
5. 查看APP日志输出

---

**文档版本**：v1.0  
**更新日期**：2024年10月16日

