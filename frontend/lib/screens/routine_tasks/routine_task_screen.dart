import 'package:flutter/material.dart';

/// 清洁任务页（占位）
class RoutineTaskScreen extends StatelessWidget {
  const RoutineTaskScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('清洁任务')),
      body: const Center(
        child: Text('清洁任务 - 待实现'),
      ),
    );
  }
}

