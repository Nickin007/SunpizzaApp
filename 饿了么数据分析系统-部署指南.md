# 饿了么门店数据分析系统 - 部署指南

## 系统概述

该系统实现了饿了么门店数据的Excel导入、存储、分析和展示功能，包含8个功能模块：
1. 数据上传
2. 数据库编辑
3. 基础信息分析
4. 运营时长分析
5. 订单财务分析
6. 营销漏斗分析
7. 商品运营分析
8. 服务质量分析

## 部署步骤

### 1. 数据库迁移

在服务器上执行数据库迁移脚本：

```bash
# 登录服务器
ssh ubuntu@118.89.73.199

# 进入项目目录
cd ~/SunpizzaApp

# 激活虚拟环境
source backend/venv/bin/activate

# 执行迁移脚本
python3 deploy/migrate_eleme_store_data.py
```

迁移脚本会创建3个表：
- `eleme_store_daily_data`: 门店每日数据表（80+字段）
- `eleme_import_logs`: 数据导入日志表
- `eleme_field_config`: 字段配置表（用于数据库编辑功能）

### 2. 安装Python依赖

如果需要，安装pandas库（用于Excel解析）：

```bash
pip install pandas openpyxl
```

### 3. 上传后端文件

上传以下新增文件到服务器：

```bash
# 数据模型（已更新）
scp backend/app/models.py ubuntu@118.89.73.199:~/SunpizzaApp/backend/app/

# Excel解析服务
scp backend/app/services/excel_parser.py ubuntu@118.89.73.199:~/SunpizzaApp/backend/app/services/

# API蓝图
scp backend/app/api/eleme.py ubuntu@118.89.73.199:~/SunpizzaApp/backend/app/api/

# 应用初始化（已更新）
scp backend/app/__init__.py ubuntu@118.89.73.199:~/SunpizzaApp/backend/app/

# 迁移脚本
scp deploy/migrate_eleme_store_data.py ubuntu@118.89.73.199:~/SunpizzaApp/deploy/
```

### 4. 重启后端服务

```bash
sudo systemctl restart sunpizza-backend
sudo systemctl status sunpizza-backend
```

### 5. 前端部署

在本地构建前端：

```bash
cd admin-web
npm run build
```

上传前端文件到服务器：

```bash
scp -r admin-web/dist/* ubuntu@118.25.70.79:~/SunpizzaApp/admin-web/dist/
```

确保权限正确：

```bash
ssh ubuntu@118.25.70.79
chmod -R 755 ~/SunpizzaApp/admin-web/dist
```

## API接口说明

### 基础接口
- `POST /api/eleme/upload` - 上传Excel文件
- `GET /api/eleme/import-logs` - 获取导入日志
- `GET /api/eleme/data` - 获取门店数据
- `GET /api/eleme/statistics` - 获取统计数据
- `GET /api/eleme/cities` - 获取城市列表
- `GET /api/eleme/stores` - 获取门店列表

### 字段配置接口（仅管理员）
- `GET /api/eleme/fields` - 获取字段配置列表
- `POST /api/eleme/fields` - 创建字段配置
- `PUT /api/eleme/fields/:id` - 更新字段配置
- `DELETE /api/eleme/fields/:id` - 删除字段配置

## Excel文件格式要求

### 必填列
- 日期
- 门店名称

### 支持的列（约80+列）
详见 `backend/app/services/excel_parser.py` 中的 `FIELD_MAPPING` 映射表

### 文件要求
- 格式：.xlsx 或 .xls
- 大小：< 50MB
- 编码：UTF-8

## 访问地址

- 前端：http://118.25.70.79
- 后端API：http://118.89.73.199:5000/api

## 功能使用说明

### 1. 数据上传
1. 登录系统后，进入"饿了么数据分析" -> "门店分析"
2. 点击"数据上传"Tab
3. 拖拽或选择Excel文件上传
4. 系统自动解析并导入数据
5. 查看导入历史记录

### 2. 数据库编辑
1. 点击"数据库编辑"Tab
2. 查看当前字段配置
3. 可以添加、编辑、删除字段配置
4. 用于与饿了么数据结构保持同步

### 3. 数据分析
1. 选择对应的分析Tab（基础信息、运营时长等）
2. 使用顶部筛选栏筛选数据：
   - 选择日期范围
   - 选择城市
   - 选择门店
3. 查看统计卡片和数据表格
4. 点击"刷新"更新数据
5. 点击"导出"下载报表

## 注意事项

1. **权限要求**：只有 `admin` 和 `delivery_operation` 角色可以访问饿了么数据分析模块
2. **文件上传**：确保服务器 `uploads/` 目录有写权限
3. **数据库字段**：如果饿了么修改了Excel列名，需要在"数据库编辑"Tab中同步更新
4. **性能优化**：大量数据时建议添加索引和分页查询
5. **数据备份**：定期备份 `eleme_store_daily_data` 表

## 故障排查

### 上传失败
- 检查文件格式和大小
- 检查服务器磁盘空间
- 查看后端日志：`sudo journalctl -u sunpizza-backend -n 50`

### 数据未显示
- 检查日期筛选范围
- 确认数据已成功导入（查看导入日志）
- 检查浏览器控制台错误

### API错误
- 检查后端服务状态：`sudo systemctl status sunpizza-backend`
- 检查数据库连接
- 查看错误日志

## 后续扩展

### 图表功能
可以集成 ECharts 实现：
- 营业额趋势折线图
- 门店对比柱状图
- 营销漏斗图
- 热力图
- 雷达图

### 数据导出
可以实现：
- Excel导出
- PDF报表导出
- 定时邮件报告

### 高级分析
可以添加：
- 同比/环比分析
- 异常数据预警
- 趋势预测
- 自定义报表

## 联系支持

如有问题，请联系系统管理员。

