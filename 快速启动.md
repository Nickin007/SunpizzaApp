# 🚀 圣比萨APP - 快速启动指南

## ⚡ 超简单启动流程

### 第一步：准备工作

1. **确保MySQL服务已启动**
2. **配置 `.env` 文件**

在 `backend` 目录创建 `.env` 文件：

```env
SECRET_KEY=sunpizza-dev-secret-key-2025
JWT_SECRET_KEY=sunpizza-jwt-secret-2025

MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=你的MySQL密码
MYSQL_DATABASE=sunpizza_db

FLASK_ENV=development
FLASK_DEBUG=1
```

**重要**：修改 `MYSQL_PASSWORD` 为你的MySQL密码！

---

### 第二步：启动后端（只需3条命令）

打开命令行，进入后端目录：

```bash
cd C:\Users\YQH20\Desktop\SunpizzaApp\backend
```

**1. 创建虚拟环境并安装依赖**：
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**2. 一键初始化数据库**（自动创建数据库和所有表）：
```bash
python init_db.py
```

运行后会看到：
```
============================================================
🚀 圣比萨智能门店管理平台 - 数据库初始化
============================================================

📡 正在连接到MySQL服务器...
📦 正在创建数据库 'sunpizza_db'...
✅ 数据库创建成功

📋 正在创建数据库表...
✅ 数据库表创建成功

📚 正在初始化字典表...
✅ 字典表初始化成功

👤 正在创建默认管理员账户...
  ✅ 管理员账户创建成功
     用户名: admin
     密码: admin123

🎲 正在创建示例数据...
  📍 创建示例门店...
  👔 创建区域经理...
  👨‍💼 创建店长账户...
  🧹 创建清洁任务模板...
  📚 创建培训分类...
  ✅ 示例数据创建成功

============================================================
🎉 数据库初始化完成！
============================================================

📝 测试账号：
  管理员    - 用户名: admin         密码: admin123
  区域经理  - 用户名: manager1      密码: 123456
  店长1     - 用户名: shopmanager1  密码: 123456
  店长2     - 用户名: shopmanager2  密码: 123456

🚀 现在可以启动后端服务了: python app.py
============================================================
```

**3. 启动后端服务**：
```bash
python app.py
```

看到 `Running on http://127.0.0.1:5000` 就成功了！

**保持这个窗口打开！**

---

### 第三步：启动前端（只需2条命令）

打开**新的命令行窗口**，进入前端目录：

```bash
cd C:\Users\YQH20\Desktop\SunpizzaApp\frontend
```

**1. 安装依赖**：
```bash
flutter pub get
```

**2. 启动应用**：
```bash
flutter run -d chrome
```

等待编译完成，浏览器会自动打开！

---

### 第四步：登录测试

使用以下任意账号登录：

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 区域经理 | manager1 | 123456 |
| 店长1 | shopmanager1 | 123456 |
| 店长2 | shopmanager2 | 123456 |

---

## 🎯 你将看到什么

登录成功后，底部有4个导航标签：

### 🏠 首页
- 4个彩色统计圆环（待受理、进行中、已完成、已关闭）
- 点击圆环直接跳转到对应工单列表
- 快捷入口：创建工单、今日清洁
- 最新动态列表

### 📋 店务
- 顶部4个状态标签页
- 工单卡片列表（带类型标签、优先级、截止日期）
- 右下角浮动"+"按钮创建新工单

### 🎓 培训
- 顶部分类标签（全部、产品类、服务类、运营类）
- 课程网格展示
- 学习状态标识（未开始、学习中、已通过）

### 👤 我的
- 个人信息展示
- 工作数据入口
- 系统设置
- 退出登录

---

## ⚙️ 常见问题

### Q1: 数据库已存在怎么办？

再次运行 `python init_db.py` 时，会提示：

```
✅ 数据库 'sunpizza_db' 已存在

⚠️  是否要删除并重新创建数据库？这将清空所有数据！(yes/no):
```

- 输入 `yes` - 删除并重新创建（清空所有数据）
- 输入 `no` - 保留现有数据

### Q2: MySQL连接失败

```
❌ 连接MySQL失败
```

**检查清单**：
1. ✅ MySQL服务是否已启动？
2. ✅ `.env` 文件中的密码是否正确？
3. ✅ MySQL用户名是否正确？

### Q3: 虚拟环境激活失败

如果 `venv\Scripts\activate` 报错，尝试：

```bash
# PowerShell
venv\Scripts\Activate.ps1

# 如果提示执行策略错误
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q4: Flutter依赖下载慢

使用国内镜像：

```bash
# PowerShell
$env:PUB_HOSTED_URL="https://pub.flutter-io.cn"
$env:FLUTTER_STORAGE_BASE_URL="https://storage.flutter-io.cn"

flutter pub get
```

---

## 🔄 下次启动

以后启动项目只需：

**后端**：
```bash
cd backend
venv\Scripts\activate
python app.py
```

**前端**（新窗口）：
```bash
cd frontend
flutter run -d chrome
```

---

## 🛑 停止服务

- 后端：在命令行按 `Ctrl + C`
- 前端：在命令行按 `Ctrl + C` 或关闭浏览器

---

## 📚 更多文档

- 完整文档：查看 `docs/` 目录
- API文档：`docs/API文档.md`
- 数据库设计：`docs/数据库设计.md`

---

**🎉 现在开始体验吧！**

